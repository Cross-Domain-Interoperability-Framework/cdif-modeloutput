# cdif-modeloutput
The repository holds the output for production processes generating syntax-bound artefacts for CDIF profiles from the CDIF UML model. There are two related repositories: cdif-umlmodel holds the UML models, and cdif-productionconfiguration holds the configuration information for the production transformations. 
